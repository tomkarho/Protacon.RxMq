trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: NuGetCommand@2
  displayName: 'Pack Legacy library'
  inputs:
    command: 'pack'
    packagesToPack: 'Protacon.RxMq.AzureServiceBusLegacy.csproj/Protacon.RxMq.AzureServiceBusLegacy.csproj.csproj'
    configuration: '$(buildConfiguration)'

- task: NuGetCommand@2
displayName: 'Pack Modern library'
  inputs:
    command: 'pack'
    packagesToPack: 'Protacon.RxMq.AzureServiceBus.csproj/Protacon.RxMq.AzureServiceBus.csproj.csproj'
    configuration: '$(buildConfiguration)'
